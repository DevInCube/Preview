int multDigits(int number) {
	//while(1);
	int a = 10;
	int b = 1/ (a-a);
	int mult = 1;
	while(number > 0) {
		short digit = number % 10;
		if(digit > 0) 
			mult *= digit;
		number /= 10;
	}
	return mult;
}
