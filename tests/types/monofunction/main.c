#include <stdio.h>

int main ( int argc, char *argv[] )
{
  if ( argc != 2 ) /* argc should be 2 for correct execution */
  {
    return 1;
  }
  else 
  {
    int num_tests = argv[1];
    while(num_tests-- >= 0)
    {
      // 
    }
    return 0;
  }
}
